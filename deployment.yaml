apiVersion: apps/v1
kind: Deployment
metadata:
name: hello
labels:
  app: hello
spec:
strategy:
  type: Recreate
template:
  metadata:
    labels:
      app: hello
      tier: hello
  spec:
    containers:
    - name: hello
      image: iumi/hello:latest
      imagePullPolicy: Always
      ports:
      - containerPort: 5001
        name: hello

---
apiVersion: v1
kind: Service
metadata:
name: hello
labels:
  app: hello
spec:
ports:
  - port: 5001
    targetPort: 5001
selector:
  app: hello
  tier: hello
type: LoadBalancer